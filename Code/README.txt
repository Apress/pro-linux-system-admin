This directory contains configuration files and configuration file snippets.

For each chapter that uses example configuration files, we've included the
apropriate files.

You can find Red Hat specific files in a subdirectory called `redhat' and
Ubuntu specific files in a subdirectory called `ubuntu'.
